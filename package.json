{
  "name": "maghrabi-office-studio",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "start": "serve -s frontend/dist -l tcp://0.0.0.0:$PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
